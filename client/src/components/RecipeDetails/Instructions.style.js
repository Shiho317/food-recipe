import styled from "styled-components";

export const InstructionsList = styled.li`
  background: ${props => props.theme.color.orange};
  list-style: none;
  color: ${props => props.theme.color.green};
`